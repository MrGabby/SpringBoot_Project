-- SQL script to create the crop_deal database in PostgreSQL
-- Run this script in PostgreSQL if the database doesn't exist

-- Connect to PostgreSQL as superuser and run:
-- CREATE DATABASE crop_deal;

-- Or use psql command:
-- psql -U postgres -c "CREATE DATABASE crop_deal;"

-- Verify database exists:
-- \l (to list all databases)
